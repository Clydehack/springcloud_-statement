<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true" scan="true" scanPeriod="30 seconds">
    <jmxConfigurator/>

    <!-- 典型的日志pattern -->
	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
              <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

	<!-- 演示按时间滚动的策略 -->
    <appender name="defaultLogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/settlement.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/settlement.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 保留 30天数据，默认无限-->
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
              <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
	<!-- 自定义记录到mongodb的日志 -->
	<appender name="customLog" class="com.uzone.settlement.framework.log.MongoDBAppender"> </appender>
	<springProfile name="test,dev">
		<logger name="org.springframework.web" level="INFO">
			<appender-ref ref="customLog" />
		</logger>
		<logger name="com.uzone" level="INFO" />
	</springProfile>

    <root level="INFO">
        <appender-ref ref="console"/>
        <appender-ref ref="defaultLogFile"/>
    </root>
</configuration>
